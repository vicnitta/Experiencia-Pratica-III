export async function loadRoute(route) {
    const main = document.getElementById("app");
    const response = await fetch(`templates/${route}.html`);
    const html = await response.text();
    main.innerHTML = html;
}
